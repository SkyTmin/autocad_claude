# HANDOFF — последний контекст

> Этот файл — главный мост между сессиями. Читай его вместе с `README.md`, `CLAUDE.md`, `status/ROADMAP.md`, `status/ISSUES.md` и актуальными спеками.

---

## Где мы сейчас

**Дата последнего обновления**: 2026-05-11  
**Репо**: `SkyTmin/autocad_claude`  
**Ветка**: `claude/autocad-knowledge-hyVpn`  
**Главный файл**: `src/lisp/commands/sv.lsp`  
**Текущая версия файла**: `sv.lsp v13`  
**Команды**: `SV`, `SVP`

Новые коммиты этой сессии:

- `ca78878b924f7d1edfeb9f6e4f084324c654c348` — `feat: add SVP project deviation`
- `ef03a2df6a8fe928069ae723b3b7abf24577669e` — `fix: balance SVP command parentheses`
- `11553949d316105abdc2d97fe737d177cd3d3834` — `docs: update SPEC-003 for project deviation`

Предыдущие коммиты по `SVP v12`:

- `689ff3e3ccb9a041784cfd24676ee74ee0d02f7b` — `feat: add SVP pile extension command`
- `cba4618ee562baf55227c22581c16e94cdd2ed12` — `docs: add SPEC-003 pile extension`

---

## Что уже подтверждено вручную

- `SV v11` был вручную проверен в Civil 3D: по словам Шамиля, команда `SV` работает отлично.
- `SVP v12` тоже был вручную проверен: построение нового сечения на заданной отметке сработало как надо.
- `SVP v13` реализован, но еще не проходил ручной прогон в Civil 3D после добавления проектных отклонений.

---

## Что делает `SV`

`SV` — стабильная команда для расчета отклонения сваи по двум сечениям:

- режим `1`: пользователь рамкой выбирает все точки, код сам группирует сваи по XY и сечения по Z;
- режим `2`: пользователь вручную выбирает нижнее и верхнее сечения;
- строятся окружности нижнего и верхнего сечений;
- считается смещение верхнего центра относительно нижнего;
- рисуются стрелки X/Y и подписи в `мм/1м`;
- важное исправление v11: `gc-pile-strip-hm` удаляет пару отметчиков высоты только если она явно изолирована над сечением.

`SV` в рамках v12/v13 не меняли по поведению намеренно. После изменений `SVP` все равно нужен regression check команды `SV`.

---

## Что делает `SVP v13`

`SVP` — команда продления/интерполяции сваи до введенной Z-отметки и расчета проектного отклонения до нового сечения.

Сценарий пользователя:

1. Запустить `SVP`.
2. Выбрать съемочные точки свай рамкой так же, как в `SV` режим `1`.
3. Ввести целевую высотную отметку Z.
4. Выбрать рамкой проектные объекты: `CIRCLE`, `POINT`, `AECC*POINT`, `AEC*POINT`.

Поведение:

- для каждой сваи находятся нижнее и верхнее сечения по текущей логике `SV v11`;
- центр на целевой отметке считается по формуле:

```text
k = (Ztarget - Zlow) / (Zhigh - Zlow)
Xtarget = Xlow + k * (Xhigh - Xlow)
Ytarget = Ylow + k * (Yhigh - Ylow)
Ztarget = введенная отметка
```

- интерполяция и экстраполяция выше/ниже фактической съемки разрешены;
- новое сечение строится окружностью `R = 0.710` на слое `GC-Сваи-Срез`;
- если проектные объекты выбраны, для каждого нового центра берется ближайший неиспользованный проектный центр в радиусе `*gc-pile-project-match-max* = 2.000` м;
- для `CIRCLE` проектный центр берется из центра окружности, для точек — из XY координат;
- Z проектного объекта игнорируется, графика проектного отклонения рисуется на Z среза;
- `dX = Xnew - Xproject`, `dY = Ynew - Yproject`;
- подписи проектного отклонения показываются в целых абсолютных миллиметрах, без деления на метр;
- направление задают стрелки X/Y от проектного центра в сторону нового центра;
- слои проектной графики: `GC-Сваи-Проект-Отклонения`, `GC-Сваи-Проект-Текст`;
- если ближайший проектный центр дальше 2 м, сечение все равно строится, а проектное отклонение для этой сваи пропускается с сообщением в консоль.

Если проектные объекты не выбраны, `SVP` строит только новые сечения, как v12.

---

## Связанные спеки

- `specs/001-pile-deviation.md` — базовая логика `SV`: окружности, стрелки, подписи.
- `specs/002-pile-multi-batch.md` — batch-обработка нескольких свай одной рамкой.
- `specs/003-pile-extension.md` — `SVP`: продление/интерполяция сваи и проектное отклонение v13.

---

## Открытые вопросы и долг

### Открытый issue #001

Свая с недосэмплированным сечением из 2 снятых точек пока не обрабатывается, потому что круг строится только по `>=3` точкам.

Варианты решения:

1. Оставить как рабочее ограничение и просить снимать минимум 3 точки на сечение.
2. Отдельной итерацией добавить построение окружности по 2 точкам + `*gc-pile-r-norm*`.

Вариант 2 требует отдельного ADR/спеки: это геометрическое допущение, а не мелкий фикс. `SVP v13` этот вопрос намеренно не решает.

---

## Что делать следующей сессии первым делом

1. Прочитать `README.md`, `CLAUDE.md`, этот `HANDOFF.md`, `status/ROADMAP.md`, `status/ISSUES.md`, `specs/001-pile-deviation.md`, `specs/002-pile-multi-batch.md`, `specs/003-pile-extension.md`, `src/lisp/commands/sv.lsp`.
2. Загрузить `src/lisp/commands/sv.lsp` в Civil 3D и проверить сообщение `sv.lsp v13 загружен. Команды: SV, SVP`.
3. Ручной тест `SVP v13` с проектными кругами:
   - выбрать съемочные точки свай;
   - ввести Z выше верхней съемки;
   - выбрать проектные окружности;
   - проверить окружности `R=0.710` на `GC-Сваи-Срез` и стрелки/подписи на Z среза.
4. Повторить `SVP v13` с проектными точками вместо кругов.
5. Проверить несколько свай за один запуск и nearest-match без повторного использования проектного центра.
6. Проверить дальний/лишний проектный объект: свая не должна получить ложное отклонение, если ближайший центр дальше 2 м.
7. Сделать regression check `SV`, чтобы подтвердить, что стабильное поведение v11 не сломалось.

---

## Известные риски

- Нет автоматической среды Civil 3D в CI: финальную приемку делает Шамиль вручную.
- `SVP v13` статически проверен на баланс скобок после фикса, но полноценная загрузка AutoLISP возможна только в AutoCAD/Civil 3D.
- `#001` остается известным ограничением для сечений из 2 точек.
