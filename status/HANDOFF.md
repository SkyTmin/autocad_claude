# HANDOFF — последний контекст

> Этот файл — главный мост между сессиями. Читать его вместе с `README.md`, `CLAUDE.md`, `status/ROADMAP.md`, `status/ISSUES.md` и актуальными спеками.

---

## Где мы сейчас

**Дата последнего обновления**: 2026-05-11  
**Репо**: `SkyTmin/autocad_claude`  
**Ветка**: `claude/autocad-knowledge-hyVpn`  
**Главный файл**: `src/lisp/commands/sv.lsp`  
**Текущая версия файла**: `sv.lsp v14`  
**Команды**: `SV`, `SVP`

Текущая рабочая база:

- `SV v11` вручную подтвержден в Civil 3D: команда `SV` работала отлично.
- `SVP v12` вручную подтвержден: построение нового сечения на заданной Z-отметке сработало как надо.
- `SVP v13` вручную подтвержден: проектные отклонения от проектного центра к новому сечению сработали.
- `SV v14` реализован: добавлен режим `SV 3`, который делает `SV 1 + SVP` за один запуск. Нужен ручной прогон в Civil 3D.

---

## Последние коммиты

- `5fa9853ed480c3ba5bca9bf7cade514fff9cd755` — `feat: add SV mode 3 combined workflow`
- `a924e6bbd7ca7d09f1bc0d6fa47f1e4af4db6af7` — `docs: add SPEC-004 SV combined mode`

Предыдущие важные коммиты по `SVP`:

- `ca78878b924f7d1edfeb9f6e4f084324c654c348` — `feat: add SVP project deviation`
- `ef03a2df6a8fe928069ae723b3b7abf24577669e` — `fix: balance SVP command parentheses`
- `11553949d316105abdc2d97fe737d177cd3d3834` — `docs: update SPEC-003 for project deviation`
- `4c2e6d4b2a9844c906eab6ef60f6bdd61446a5af` — `docs: update handoff for SVP v13`
- `be40e1a29df0f2f42b49a06534f4d69463443499` — `docs: record SVP v13 manual success`

---

## Что делает `SV`

`SV` — команда обработки свай по двум фактическим сечениям.

Режимы:

1. `SV 1`: пользователь рамкой выбирает все точки, код сам группирует сваи по XY и сечения по Z.
2. `SV 2`: пользователь вручную выбирает нижнее и верхнее сечение.
3. `SV 3`: новый объединенный режим `SV 1 + SVP`.

Базовое поведение `SV 1/2`:

- строятся окружности нижнего и верхнего сечений;
- считается смещение верхнего центра относительно нижнего;
- рисуются стрелки X/Y и подписи в `мм/1м`;
- исправление v11 сохраняется: `gc-pile-strip-hm` удаляет пару отметчиков высоты только если она явно изолирована над сечением.

---

## Что делает `SVP v13`

`SVP` — отдельная команда продления/интерполяции сваи до введенной Z-отметки.

Сценарий:

1. Запустить `SVP`.
2. Выбрать съемочные точки свай рамкой так же, как в `SV 1`.
3. Ввести целевую высотную отметку Z.
4. Выбрать проектные объекты: `CIRCLE`, `POINT`, `AECC*POINT`, `AEC*POINT`.

Поведение:

- нижнее и верхнее сечения находятся по логике `SV v11`;
- центр на целевой отметке считается по линейной интерполяции/экстраполяции;
- новое сечение строится окружностью `R = 0.710` на слое `GC-Сваи-Срез`;
- проектное отклонение считается как `dX = Xnew - Xproject`, `dY = Ynew - Yproject`;
- подписи проектного отклонения — абсолютные целые миллиметры, без деления на метр;
- Z проектного объекта игнорируется, графика отдельного `SVP` рисуется на Z среза.

---

## Что делает `SV 3 v14`

`SV 3` — объединенный режим, который делает `SV 1` и `SVP` за один запуск.

Сценарий пользователя:

1. Запустить `SV`.
2. Выбрать режим `3`.
3. Рамкой выбрать все съемочные точки свай.
4. Ввести целевую Z-отметку среза.
5. Рамкой выбрать проектные круги или проектные точки.

Поведение `SV 3`:

- использует одну группировку свай из `SV 1`;
- строит нижнее и верхнее фактические сечения;
- строит новое сечение на введенной Z-отметке, как `SVP`;
- синие отклонения `SV` рисует на `Zlow + 1.700`;
- белые проектные отклонения `SVP` рисует на `Ztarget + 2.600`;
- отдельные `SV 1`, `SV 2` и `SVP` должны сохранить прежнее поведение.

Слои `SV 3`:

- `GC-Сваи-SV3-Синие-Отклонения`, цвет `5`;
- `GC-Сваи-SV3-Синий-Текст`, цвет `5`;
- `GC-Сваи-SV3-Проект-Отклонения`, цвет `7`;
- `GC-Сваи-SV3-Проект-Текст`, цвет `7`.

---

## Связанные спеки

- `specs/001-pile-deviation.md` — базовая логика `SV`: окружности, стрелки, подписи.
- `specs/002-pile-multi-batch.md` — batch-обработка нескольких свай одной рамкой.
- `specs/003-pile-extension.md` — `SVP`: продление/интерполяция сваи и проектное отклонение v13.
- `specs/004-sv-combined.md` — `SV 3`: объединенный режим `SV 1 + SVP` v14.

---

## Открытые вопросы и долг

### Открытый issue #001

Свая с недосэмплированным сечением из 2 снятых точек пока не обрабатывается, потому что круг строится только по `>=3` точкам.

Варианты решения:

1. Оставить как рабочее ограничение и снимать минимум 3 точки на сечение.
2. Отдельной итерацией добавить построение окружности по 2 точкам + `*gc-pile-r-norm*`.

Вариант 2 требует отдельной спеки: это геометрическое допущение, а не мелкий фикс. `SVP v13` и `SV 3 v14` этот вопрос намеренно не решают.

---

## Что делать следующей сессии первым делом

1. Прочитать `README.md`, `CLAUDE.md`, этот `HANDOFF.md`, `status/ROADMAP.md`, `status/ISSUES.md`, `specs/001-pile-deviation.md`, `specs/002-pile-multi-batch.md`, `specs/003-pile-extension.md`, `specs/004-sv-combined.md`, `src/lisp/commands/sv.lsp`.
2. В Civil 3D загрузить `src/lisp/commands/sv.lsp` и убедиться, что сообщение загрузки: `sv.lsp v14 загружен`.
3. Запустить `SV`, выбрать режим `3`.
4. Выбрать съемочные точки свай, ввести Z, выбрать проектные круги/точки.
5. Проверить: синие стрелки и цифры `SV` находятся на `Zlow + 1.700`, белые проектные стрелки и цифры находятся на `Ztarget + 2.600`.
6. Сделать regression check: `SV 1`, `SV 2`, отдельный `SVP`.
7. Если ручной прогон успешен, зафиксировать это в `HANDOFF.md` отдельным docs-коммитом.

---

## Известные риски

- Нет автоматической среды Civil 3D в CI: финальную приемку делает Шамиль вручную.
- `#001` остается известным ограничением для сечений из 2 точек.
- В `v14` общая отрисовка отклонений была вынесена в общий helper, поэтому особенно важен regression check `SV 1`, `SV 2`, `SVP`.
