# HANDOFF — последний контекст

> Этот файл — самый важный артефакт между сессиями. Любая нейросеть, читающая
> его, должна понять с полу-слова: где мы, что сделано, что дальше.
> **Обновляй в конце каждой сессии.**

---

## Где мы сейчас

**Дата последнего обновления**: 2026-05-03
**Сессия**: #1 (фундамент + SPEC-001 v1→v2→v3 + SPEC-002 v1→v2→v3)
**Ветка**: `claude/autocad-knowledge-hyVpn`

### Что сделано

1. **Фундамент проекта** (Конституция, ADR-0001/0002, docs/, specs/, status/, src/, samples/, .claude/) — закоммичено в первом коммите.

2. **SPEC-001 «Обработка свай»** — согласована, потом ревизирована до v2 после
   первого прогона. Журнал ревизий — внутри `specs/001-pile-deviation.md` §11.

3. **Реализация — `src/lisp/commands/sv.lsp` v3**:
   - Извлечение координат: COGO Point / AcDbPoint через VL (Easting/Coordinates).
   - Окружность по 3 точкам, перебор C(N,3), выбор min `|R − 710|`.
   - **Выборка `ssget` с фильтром** `POINT,AECC*POINT,AEC*POINT` — посторонние
     объекты (наши круги, стрелки, текст) не попадают в выборку даже если
     захвачены рамкой. **v3 фикс root cause** бага «4 группы» при повторных запусках.
   - **Кластеризация по Z**: плотные группы (порог 50 мм) → отбор 2 групп
     с наибольшим количеством точек ≥3. **v3 фикс**: устойчиво к посторонним
     одиночным точкам, точно по словам Шамиля «минимум 3 на одной высоте».
   - Слои `GC-Сваи-*`, стиль текста с fallback'ом GOSTB→ISOCPEUR→Standard.
   - Стрелки длиной 400 мм, текст 100 мм. Подпись — одно целое число (мм/1м).
   - **Положение текста (v3)**:
     - X→ → ПОД серединой, ротация 0
     - X← → НАД серединой, ротация 0   ← **v3 фикс** (было всегда под)
     - Y↑ → СЛЕВА, ротация 90°
     - Y↓ → СПРАВА, ротация 90°
   - Все константы (длина, высота, порог, фильтр) вынесены в начало файла.

4. **Прогоны Шамиля**:
   - #1: SV 1 работало только на ровно 11 точках, ломалось на >11. SV 2 —
     разные значения оказались user error.
   - #2 (v2): подтверждено что выборка хватает посторонние объекты при
     повторных запусках. Текст для X← неправильно (под, а не над).

5. **SPEC-002 v1 не сработала** — single-linkage склеивает соседние сваи.
6. **SPEC-002 v2 не сработала на реальном sample** — глобальная Z-кластеризация
   неверно работает для свай на разной глубине: 2 самые большие группы по Z
   ≠ «низы всех» и «верхи всех», когда сваи погружены на разные глубины.
7. **SPEC-002 v3 — переписана** на per-section search:
   - Z-кластеризация удалена.
   - Жадно перебираем C(N,3) троек по всему массиву; тройка валидна если
     Z трёх точек одинаков (Δ ≤ 50 мм) И окружность по XY с R≈710 мм.
   - Все точки в радиусе R+ε от центра НА ТОМ ЖЕ Z = одно сечение.
   - Удаляем, повторяем — получаем список ВСЕХ сечений.
   - Группируем в сваи по близости XY-центров (≤0.5м); в свае: min Z = низ,
     max Z = верх; dZ ∈ [0.3; 5.0] м.
   - **Также текст**: с дальнего угла квадранта (0.4м) на середину стрелки
     (along=0.2м, lateral=0.1м) — фидбек Шамиля «надо рядом со стрелочкой».

8. **Реальный sample сохранён**: `samples/csv/2026-05-03-pile-sample-multi.csv`
   (106 точек, ~10 свай на Z от 0.5 до 4.0 м).

### На чём остановились

Жду **прогон v3 на реальном sample**:
- На реальной выборке должны нарисоваться все сваи (а не половина).
- Текст должен быть рядом со стрелками.
- Положение в квадрантах сохранено (формула с `sxy` та же, изменились
  только дистанции: along/lateral вместо одного offset).

---

## Что дальше (следующая сессия делает первым)

1. Прочитать `CLAUDE.md`, этот `HANDOFF.md`, `ROADMAP.md`,
   `specs/001-pile-deviation.md`, `specs/002-pile-multi-batch.md`,
   `src/lisp/commands/sv.lsp`.
2. Узнать у Шамиля: **результат прогона** SPEC-002 (несколько свай) и
   SPEC-001 v3 (одна свая через тот же mode 1).
3. Если работает — закрыть обе спеки, обновить QUALITY.md, переходить
   к следующей фиче из ROADMAP/Backlog.
4. Если есть баги — **root cause** (R3), правки, ревизии спек.

**Известные риски для SPEC-002**:
- Точки разных свай ближе порога XY (1.0м) — кластеры слипнутся.
  Параметр в `*gc-pile-xy-cluster*`, можно подкрутить.
- Если на одной свае нижнее и верхнее сечения сильно сместились в плане
  (наклон сваи >0.5м в плане) — пара может не сопоставиться.
  Параметр в `*gc-pile-pair-tolerance*`.
- single-linkage XY-кластеризация — `O(n²·k)`, может тормозить на 100+ точках.
  Если станет проблемой — оптимизация на отдельной итерации.

---

## Открытые вопросы

- Версия Civil 3D у Шамиля (на случай специфичных багов API).
- Установить ли скилл `session-start-hook` (`status/BACKLOG.md`).

---

## Известные риски

- **Нет среды для авто-проверки**: только Шамиль может прогнать в Civil 3D.
- **DXF SOLID для наконечника**: возможны проблемы с порядком вершин.
- **Шрифт GOSTB**: если нет — fallback на Standard, визуально может отличаться.
- **AI-лень**: митигация — самопроверка DoD, KPI в QUALITY.md.

---

## Открытые ISSUES

Нет на момент написания.

---

## Шаблон обновления

В конце сессии переписать секции выше. Не накапливать историю — её
сохраняет git. `HANDOFF.md` всегда отражает **последнее** состояние.
